NODE_ENV=development
BACKEND_PORT=3001
FRONTEND_PORT=3000
DATABASE_URL=postgresql://app_user:replace_me@localhost:5432/freelancer_manager
DB_SSL=false
JWT_SECRET=replace-with-at-least-32-random-characters
OPENROUTER_API_KEY=
EXPOSE_DEMO_CREDENTIALS=false
# Local-only demo autofill. The ignored .env must provide one complete
# DEMO_*, SEED_ADMIN_*, ADMIN_*, or DEFAULT_* email/password pair.
ENABLE_DEMO_CREDENTIAL_AUTOFILL=false
